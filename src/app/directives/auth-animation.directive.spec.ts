import {AuthAnimationDirective} from './auth-animation.directive';

describe('AuthAnimationDirective', () => {
    // it('should create an instance', () => {
    //   const directive = new AuthAnimationDirective();
    //   expect(directive).toBeTruthy();
    // });
});
